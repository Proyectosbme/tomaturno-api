# =========================
# Puerto y contexto HTTP
# =========================
quarkus.http.port=${HTTP_PORT}
quarkus.banner.path=banner.txt
quarkus.http.host=${HTTP_HOST}

# =========================
# HTTPS / SSL
# En produccion nginx hace SSL termination, el backend corre HTTP
# =========================
quarkus.http.ssl-port=0
quarkus.http.insecure-requests=${HTTP_INSECURE_REQUESTS:disabled}

%dev.quarkus.http.ssl.certificate.key-store-file=quarkus.jks
%dev.quarkus.http.ssl.certificate.key-store-password=changeit
%dev.quarkus.http.ssl.certificate.key-store-provider=JKS

# =========================
# Base de datos PostgreSQL
# =========================
quarkus.datasource.db-kind=postgresql
quarkus.datasource.username=${DB_USER}
quarkus.datasource.password=${DB_PASS}
quarkus.datasource.jdbc.url=jdbc:postgresql://${DB_HOST}:${DB_PORT}/${DB_NAME}
quarkus.hibernate-orm.database.default-schema=${DB_SCHEMA}

# =========================
# Hibernate
# ADVERTENCIA: usar 'drop-and-create' solo en desarrollo, en prod usar 'validate' o 'none'
# =========================
quarkus.hibernate-orm.database.generation=${DB_GENERATION:validate}
quarkus.hibernate-orm.sql-load-script=no-file

quarkus.http.cors=${HTTP_CORS}

# =========================
# Keycloak Admin Client
# =========================
quarkus.keycloak.admin-client.server-url=${KC_SERVER_URL}
quarkus.keycloak.admin-client.realm=${KC_ADMIN_REALM}
quarkus.keycloak.admin-client.client-id=${KC_ADMIN_CLIENT_ID}
quarkus.keycloak.admin-client.grant-type=PASSWORD
quarkus.keycloak.admin-client.username=${KC_ADMIN_USER}
quarkus.keycloak.admin-client.password=${KC_ADMIN_PASS}
tomaturno.keycloak.realm=${KC_REALM}

# =========================
# OIDC (autenticacion JWT)
# =========================
quarkus.oidc.auth-server-url=${KC_SERVER_URL}/realms/${KC_REALM}
quarkus.oidc.client-id=${OIDC_CLIENT_ID}
quarkus.oidc.credentials.secret=${OIDC_CLIENT_SECRET}
quarkus.oidc.application-type=service
quarkus.oidc.token.principal-claim=preferred_username

# Keycloak esta corriendo — OIDC habilitado tambien en dev
%dev.quarkus.oidc.auth-server-url=${KC_SERVER_URL}/realms/${KC_REALM}
# En dev se acepta cualquier issuer para poder acceder desde localhost o IP de red
%dev.quarkus.oidc.token.issuer=any

# En dev se permite HTTP plano para que el proxy del frontend funcione
%dev.quarkus.http.insecure-requests=enabled
# En dev escucha en todas las interfaces para que otros dispositivos en la red puedan conectarse
%dev.quarkus.http.host=0.0.0.0

# En prod escucha en todas las interfaces (requerido en Docker)
%prod.quarkus.http.host=0.0.0.0
